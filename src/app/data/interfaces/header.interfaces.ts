export interface Category {
	id: string;
	name: string;
	isItemLead: boolean;
}

export interface Demo {
	id: string;
	backgroundImage: string;
	title: string;
	isHidden: boolean;
}

export interface Element {
	id: string;
	name: string;
}
